{
  "name": "fe",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-test": "esbuild src/app.js --outdir=dist/js --bundle --servedir=dist --watch --metafile=metaFile.json",
    "dev": "node src/serve.js --profile=develop"
  },
  "keywords": [],
  "author": "nope <nyg1230@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.20.1",
    "esbuild-plugin-css-modules": "^0.3.0"
  }
}
